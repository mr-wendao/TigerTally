# 借鉴与致谢

本项目在设计和思路上参考了若干优秀开源项目。**本文件如实列明每一条来源。**

> **一句话说清法律状态**：我们**没有复制任何项目的源代码**，所有代码均为独立实现。
> 下表中列出的是**设计思路**的参考对象。相关项目均为宽松协议（MIT / Apache-2.0），
> 允许自由使用、修改与商用；我们仍然在此逐条致谢，因为**该谢的要谢清楚**。

---

## 一、Hermes Agent

- **项目**：[NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent)
- **协议**：MIT
- **作者**：Nous Research

**参考了它的这些设计：**

| 设计点 | 我们的理解与采用 |
|---|---|
| **Bot Mode** —— 每个 Agent 有名字、有头像，能拉群、能互相发消息 | 项目立项的直接起因。我们做的是同类事的轻量自托管版本 |
| **确定性头像** —— 由名字算出图案，不存图片素材、不联网 | 已采用。我们用 FNV-1a 哈希 + 镜像 identicon 独立实现 |
| **成员名册 + 在线状态** | 已采用 |
| **有界轮次** —— 群内成员数、轮次、消息条数均有硬上限 | 采用其思想：多 Agent 自由对话会失控，必须有界 |
| **发言顺序由规则决定，而非模型决定** | 采用。确定性、可复现、省钱 |
| **跨机器授权用签名（HMAC），而非开放总线** | 采用其思想。这是"引 Agent 进群前必须先关门"的依据 |
| **受保护的指令文件** —— Agent 不得擅自修改自己的行为准则 | 采用其思想。参考 [NousResearch/hermes-agent 的发布说明] |

**未采用**：其独立房间服务、SQLite 事件日志的具体实现（我们另行设计）。

---

## 二、Octop

- **项目**：[TencentCloud/Octop](https://github.com/TencentCloud/Octop)
- **协议**：MIT
- **作者**：腾讯云

**参考了它的这些设计：**

| 设计点 | 我们的理解与采用 |
|---|---|
| **房间 = 主持人的一条会话**，不为房间单独建路由层 | 采用其思想。这证明了轻量做法的可行性 —— 不需要更重的框架 |
| **成员"不同居"** —— 成员是独立个体，居于自己环境，不搬进房间 | 采用 |
| **单向扇出 + 回执扇入** | 采用 |
| **`corr_id` 关联 + 24 小时指纹去重** | 采用其思想。这正是"重复回话"问题的正解 |
| **成员之间禁止互相私聊，一切经房间** | 采用其思想。比"轮次上限"更硬的结构性防环手段 |
| **工具护栏（`tool_guard`）** —— YAML 规则约束工具调用权限 | 采用其思想，纳入一期安全设计 |
| **SSRF 防护** —— 防止 Agent 被诱导访问内网 | 采用其思想 |
| **主动性关怀服务** —— 定时/随机间隔主动发起关怀消息，含活跃时段、轻量模型、字数上限、去重 | 本项目另一场景（日常关怀消息）参考了其设计细节 |

**未采用**：其技术栈（FastAPI + LangGraph + Postgres）、前端、桌面端、插件市场 —— 体量与需求不匹配。

---

## 三、ntfy

- **项目**：[binwiederhier/ntfy](https://github.com/binwiederhier/ntfy)
- **协议**：Apache-2.0
- **作者**：Philipp C. Heckel

**参考与使用情况：**

第一版原型使用 ntfy 的公开服务作为消息传输。**它的"一条 HTTP 即可发布消息"的极简协议，
启发了本项目"一条 HTTP 即可加入群聊"的核心设计目标。**

**为什么在第二版中不再使用**：
ntfy 是优秀的**推送通知**服务，其定位（及默认配置）是"消息推送后短期缓存"，而非持久会话。
群聊需要成员身份、权限管理与长期消息留存 —— 这些不是它的设计目标。
**我们仍然感谢它让第一版原型得以在极短时间内跑通。**

> 注：ntfy 支持自托管，本项目**不排除**在未来版本中提供 ntfy 作为可选传输适配器。

---

## 四、ESP-Claw

- **项目**：[espressif/esp-claw](https://github.com/espressif/esp-claw)
- **协议**：Apache-2.0
- **作者**：乐鑫信息科技（Espressif）

**参考了它的这些设计：**

| 设计点 | 我们的理解与采用 |
|---|---|
| **设备侧的能力模型**（Capability 映射为可调用工具） | 参考 |
| **`cap_http_request`** —— 设备侧仅支持白名单出站 HTTP，**无 MQTT、无 WebSocket 客户端** | ⭐ **关键约束**。这是我们坚持在实时通道之外**必须保留 HTTP 接入路径**的直接依据 —— 否则物联网设备无法进群 |
| **本地 IM 扩展点** | 参考 |

**本项目与 ESP-Claw 的关系**：我们**不是**它的分支或衍生，它是一个**目标接入端**
—— 即"希望能在群里说话的设备之一"。

---

## 五、其他调研过的项目

以下项目在选型阶段被调研，**但未采用其设计**：

| 项目 | 协议 | 结论 |
|---|---|---|
| Mattermost / Zulip / Matrix Synapse | 各自协议 | 成熟的完整聊天平台，体量与场景不匹配（本项目要的是极轻量） |
| ChatDev / nanobot 等 | MIT | 学术研究或多 Agent 工作台定位，与"人机同群"场景不同 |
| Octop 生态内的 `harness-agent` 等独立包 | 见上游 | 未使用 |

---

## 六、技术上使用的标准与组件

以下为**公开标准或通用组件**，非特定项目的思路借鉴：

- **WebSocket**（RFC 6455）—— 浏览器原生实时通信标准
- **HTTP** —— 最广泛的通用协议，设备的接入路径
- **SQLite** —— 嵌入式数据库
- **Docker / Docker Compose** —— 部署方式
- **identicon** —— 一种公开的、由哈希值生成头像图案的通用技法

---

## 七、我们的承诺

1. **不复制**：本项目代码为独立实现。
2. **如实标注**：若未来引入任何第三方代码或资产，将在本文件及相应源码处标注来源与协议。
3. **接受纠正**：若哪位作者认为本项目的表述有误或引用不当，请提 Issue，我们会立即更正。

---

*最后更新：2026-09-23*
