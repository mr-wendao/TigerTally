# 任务：虎符 M0 —— 自建消息服务

## 先读这两份

- `~/tigertally/docs/DESIGN-M0.md` ← **详细设计，以它为准**
- `~/tigertally/PROJECT.md` ← 项目全貌与设计信条

## 第 0 步：环境准备（先做，否则一定卡）

```bash
sudo apt install -y golang-go
go env -w GOPROXY=https://goproxy.cn,direct
go version      # 应为 go1.22
```

**这两个坑我已经实测过了，别再踩：**

| 项 | 实测结果 |
|---|---|
| apt 里的 Go | ✅ 有 **1.22**（`2:1.22~2build1`） |
| **官方代理 proxy.golang.org** | ❌ **被墙**（连续 8 秒超时，拉不到依赖） |
| 国内镜像 goproxy.cn | ✅ 可达（200 / 50ms）—— **必须设这个** |
| gcc | ✅ 已装（可不依赖，见下） |

**用纯 Go 的 sqlite 驱动（`modernc.org/sqlite`）**，不要用 `mattn/go-sqlite3` ——
后者要 cgo，交叉编译到 Windows / macOS 会麻烦（M1 要出三平台安装包）。
纯 Go 驱动只改一个 import，换来的是后面省事。

## 目标一句话

用 Go 写一个自托管的群聊消息服务，**把消息层从第三方推送服务换到自己的机器上**。

## 硬性要求

1. **Go 实现**，编译出**单个可执行文件**（无运行时依赖，不能依赖 Node/Python）
2. **SQLite 存储**，消息**只增不删**（这是和推送服务的根本区别）
3. **双通道**：
   - HTTP —— 拉消息、发消息（设备轮询用，**必须有**）
   - WebSocket —— 实时订阅（浏览器用）
4. **每成员独立令牌**，**数据库只存哈希**（不存明文）
5. **无令牌的请求一律 401** —— 默认拒绝，不是默认开放
6. 进程崩了/重启，**消息不能丢**

## 接口（照 DESIGN-M0.md 实现）

```
POST /api/{group}/messages              发消息（Bearer 令牌）
GET  /api/{group}/messages?since=<id>   拉消息
WS   /api/{group}/ws                    实时订阅
POST /api/groups                        建群 → 返回管理员令牌
POST /api/{group}/members               加成员 → 返回成员令牌
GET  /api/{group}/members               成员列表
```

**核心原则：一条 HTTP 就是一条消息。** 不加多余概念，任何语言三行代码就能接入。

## 验收 —— 必须实测，贴真实命令与输出

**不接受"代码看起来是对的"。每一条都要跑一遍，把命令和输出贴进 `docs/M0-RESULT.md`。**

| # | 验收项 | 怎么算通过 |
|---|---|---|
| 1 | **重启不丢消息** | 发 3 条 → 杀掉进程 → 重启 → 3 条都还在 |
| 2 | 浏览器实时收到 | 两个 WebSocket 连接，一个发另一个秒收 |
| 3 | curl 也能收发 | 纯 curl 发一条、拉一条，都成功 |
| 4 | **无令牌被拒** | 不带令牌 → 401；带错误令牌 → 401 |
| 5 | 一条命令能起 | 新机器上 `./tigertally serve` 直接跑起来 |

**第 4 条是安全底线，务必真的测。** 我们这项目最怕的就是"看着有鉴权，实际没拦住"。

## 交付

```
server/                 Go 源码（干净，能直接 go build）
  go.mod
  main.go 等
docs/M0-RESULT.md       验收结果：每条验收项 + 实际命令 + 实际输出
server/README.md        怎么编译、怎么起、怎么接第一个成员
```

## 提交

**做完自己提交推送到 GitHub**（部署密钥已配好，`~/tigertally` 里直接 `git push` 即可）：

- 提交信息用中文，说清做了什么、验收结果如何
- **不要提交**编译产物（.gitignore 已挡 `server/tigertally` 等二进制）
- **不要提交**任何令牌、密钥、服务器地址、IP

## 卡住了怎么办

**两次尝试不通就停下来，写清卡在哪**，不要去改设计文档里的接口约定
—— 那是已经定好的契约，要改先回来说。
