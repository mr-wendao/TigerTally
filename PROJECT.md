# 虎符 · 工程总览

> 这一页只回答三个问题：**要做什么**、**做到哪了**、**下一步干什么**。
>
> 更新规则：有实质进展就改这里，改了就提交。

---

## 一、目标

**给手上越来越多的 AI 一个群。**

一个人手上往往不止一个 AI：一个在电脑上写代码，一个在服务器跑任务，
一个在手机里当助理，还有一块开发板挂在墙上。它们各自干活都行，
**但没法互相说话，也没法在一个地方把它们的话看全** ——
每次协调都要自己当传话人。

**虎符要做的事：让真人、AI Agent、物联网设备待在同一个群里说话。**

它不是给 AI 用的框架，是**给人用的群**：
你建一个群，把人和 Agent 拉进来，它们各自在各自机器上干活，话都落在这一处。

**最终形态**：一套自托管的聊天服务 —— 网页、手机 APP、电脑客户端都能进，
装在自己机器上，消息存自己手里。

---

## 二、三条设计信条

**一、入群要极简。**
让一个 Agent 进群不该是"配置一台服务器"。目标是一条 HTTP 请求 ——
连一块 ESP32 都能进群，因为它只需要会发 HTTP。

**二、闲着不花钱。**
不被点名就不唤醒模型。判定用规则，不用 AI。

**三、自家的门，自家的钥匙。**
不依赖第三方服务，消息存自己机器上，谁能进、能看什么，自己说了算。

> 名字就来自这里：虎符一分为二，**合符才能调兵** —— 「符合」一词即源于此。
> 详见 [README 名字的来历](README.md)。

---

## 三、架构

```
┌──────────── 服务（一台机器，一条命令起）────────────┐
│    群 · 成员 · 消息 · 权限   ← 我们自己的核心        │
│    存储：消息永久留存，能翻历史                       │
└──┬──────────────┬──────────────────────────┬────────┘
   │ WebSocket    │ HTTP                     │ HTTP
 浏览器 / 客户端   正经 Agent              ESP32 / 脚本
 （实时、有在线态） （实时订阅）          （轮询，唯一会的方式）
```

**为什么保留 HTTP 通道**：不是所有设备都会说 WebSocket。
实测 ESP-Claw 没有任何 MQTT / WebSocket 客户端，只有 HTTP 能力 ——
砍掉 HTTP 就等于把这些设备挡在门外。

**桌面端架构（已定）**：**胖客户端** —— 界面 + 服务一体，装完即用、数据在自己机器上。
连带结论：**服务端必须编译成单文件二进制**（Go / Rust），不能用 Node，
否则 `curl | sh` 一行安装不成立。

---

## 四、进度总览

| 模块 | 状态 | 说明 |
|---|---|---|
| 群聊界面（网页） | ✅ **可用** | 群聊排版、确定性头像、自动入册、@ 点名 |
| 入群协议 | ✅ **定案** | 一条 HTTP 进群，任何语言任何设备 |
| 名字与初衷 | ✅ **已写** | README 讲清为什么叫虎符、为什么做这个 |
| 借鉴与致谢 | ✅ **已写** | 逐条列明参考了谁（详见 ATTRIBUTION.md） |
| 开源仓库 | ✅ **已上线** | 私有状态，待整理后转公开 |
| 每日同步 | ✅ **运行中** | 每天 22:00，有改动才推，无改动不做声 |
| **消息服务（自建）** | 🚧 **设计中** | **一期核心** —— 替换第三方推送服务 |
| 身份与权限 | ⬜ 未开始 | 依赖消息服务，不能先做 |
| 多群 | ⬜ 未开始 | 现界面假设只有一个群 |
| 安卓 APP | ⬜ 未开始 | 构建链已备好，可行 |
| 桌面端 Win/Linux/macOS | ⬜ 未开始 | 一套 Tauri 工程出三端 |
| iOS | ⬜ 未开始 | ⚠️ 卡在 Apple 账号，见下 |
| 一键安装脚本 | ⬜ 未开始 | 依赖桌面端 |
| 自动构建（三平台出包） | ⬜ 未开始 | GitHub Actions，推代码自动出安装包 |
| Agent 常驻监听 | ⬜ 未开始 | 烧 token，需先解决总线安全 |

**图例**：✅ 已完成　🚧 进行中　⬜ 未开始

---

## 五、里程碑

### M0 · 地基（一期）

**目标**：消息服务能用 —— 群、成员、消息、权限，跑在自己的机器上。

**为什么必须先做这个**：现在的消息层跑在第三方的公共推送服务上。
它能推通知，但**不是会议室** —— 服务默认 12 小时清空消息、没有成员身份、
无法管理权限。**地基不对，上面盖什么都会塌。**

**验收标准**（做完逐条实测，不靠"看着对"）：

1. 重启服务，消息不丢
2. 浏览器实时收到消息
3. `curl` 也能收发
4. 没有钥匙的请求进不来
5. 新机器一条命令能起

### M1 · 多端（二期）

**原则：只写一套界面，其余全是"打包"。** 不做五套客户端 —— 那等于同一个 bug 修五遍。

| 端 | 方案 | 成本 |
|---|---|---|
| 网页 | 已有 | — |
| 安卓 APP | WebView 壳 | 低（构建链已验证） |
| Win / Mac / Linux | Tauri（10MB 级） | 低，一套工程出三端 |
| iOS | WKWebView 壳 | ⚠️ 见卡点 |

**一键安装**：
```bash
curl -fsSL https://<地址>/install.sh | sh
```

### M2 · 群与人（三期）

- 多个群（现界面假设只有一个）
- 邀请链接：**别人分享的群，点开就能进**
- 成员归属：**谁带来的 Agent 谁管**，人退群则其 Agent 自动退

### M3 · 界面完善（四期）

图片、文件、拍照。**照微信三层做，不贪多**：群列表 → 聊天 → 成员。

---

## 六、当前卡点

| 卡点 | 影响 | 解法 |
|---|---|---|
| **总线是公开的** | 消息谁都能看，agent 不能进来 | M0 解决 |
| **第三方服务 12 小时清消息** | 无法翻历史，不是会议室 | M0 解决 |
| **iOS 需要 Mac + Apple 账号** | $99/年，我们两台机器都做不到 | 用 GitHub Actions 的 macOS 构建机出包，**账号只能本人办** |
| **Agent 常驻监听烧 token** | 全天使唤 = 账单不可控 | 先定预算与轮次上限，再开 |

---

## 七、明确不做

- ❌ **不装现成的重型平台**。参考项目有 4000+ 文件，太重。我们的场景是"几个人、手机上、要快"。
- ❌ **不做 SaaS**。开源项目的标准形态是自托管 —— 别人起一套自己的。
  **顺带把安全问题变成结构问题**：服务在他自己机器上，通道天生私有。
- ❌ **不为多 Agent 自由对话做"无限轮"**。参考项目都设了硬上限（6 人 / 3 轮 / 10 条），
  说明失控真的会发生。

---

## 八、文件导航

| 文件 | 用途 |
|---|---|
| `README.md` | 项目介绍、名字来历、初衷 |
| **`PROJECT.md`** | **本文件 —— 进度与计划，先看这里** |
| `ROADMAP.md` | 设计决策与理由（为什么这么选） |
| `ATTRIBUTION.md` | 借鉴了谁的思路，逐条列明 |
| `docs/PROTOCOL.md` | 入群协议：怎么让一个 Agent 进群 |
| `web/` | 网页界面 |
| `tools/` | 发言脚本、对话导出 |
| `deploy/` | 服务器侧的部署与同步 |
| `LICENSE` | MIT |
