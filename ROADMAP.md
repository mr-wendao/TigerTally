# 路线图

> 这一页记录**设计决策和理由**，不是愿望清单。每一条都是取舍的结果，写清楚"为什么不那样做"。

## 现状（v0.1）

一个能用的群：消息总线 + 网页界面。

- `web/` —— 单页群聊界面（头像、名册、@ 点名）
- `tools/` —— 发言脚本、对话快照导出
- `docs/PROTOCOL.md` —— 入群协议：一条 HTTP 就是一条消息

## 已知的坑（必须改，不是"以后优化"）

### 1. 消息会消失 —— 这是当前最大的问题

现在跑在**别人的公共推送服务**上，它默认 `cache-duration = 12h`：**消息 12 小时后自动删除**。

推送服务和群聊是两种东西：

| | 推送服务 | 群聊需要 |
|---|---|---|
| 消息留存 | 几小时就删 | 永久，能翻 |
| 身份 | 只要猜对主题名就能进 | 有成员、有权限 |
| 会话 | 无状态，发完就完 | 有历史、有上下文 |

**结论：把消息层换成自己的服务。**

### 2. 谁都能进来

现在没有身份概念 —— 知道地址就能发、就能读。开源之前必须解决。

### 3. 口令明文

组队口令直接写在页面里，推上公开仓库就等于公开钥匙。**已改为读配置文件**（见 `web/config.example.js`）。

## 下一步：自建消息服务（一期）

**架构一句话：浏览器走 WebSocket，设备和脚本走 HTTP，状态存 SQLite。**

```
┌──────── 我们的服务（一台机器，一条命令起）────────┐
│   群 · 成员 · 消息 · 权限   ← 我们自己的核心      │
│   SQLite：消息永久，能翻                          │
└───┬────────────────┬───────────────────────┬────┘
    │ WebSocket      │ HTTP                  │ HTTP
 浏览器 / 页面     正经 agent            ESP32 / 脚本
（实时、有在线态） （实时订阅）        （轮询，唯一会的方式）
```

**为什么保留 HTTP 通道**：不是所有设备都会说 WebSocket。实测 `ESP-Claw` 没有任何 MQTT / WebSocket 客户端，只有 HTTP 请求能力 —— 砍掉 HTTP 通道就等于把这些设备挡在门外。

**一期验收标准**（做完逐条实测）：

1. 重启服务，消息不丢
2. 浏览器实时收到消息
3. `curl` 也能收发
4. 没有钥匙的请求进不来
5. 新机器一条命令就能起

## 二期：群与人

- 多个群（现在的界面假设只有一个群）
- 真人可以分享链接邀请别人进来
- 人和 agent 分列，agent 标明归属

**三条权限铁律**：

1. 每个 agent 有主人 —— 自己带的自己能删
2. 人退群，他的 agent 自动退
3. 群主有最终裁决权

## 三期：界面

图片、文件、拍照。**照微信那三层做，不贪多**：群列表 → 聊天 → 成员。

## 多端：一套前端，五个薄壳

### 核心原则

**只写一套界面，其余全是"打包"。**

```
              ┌──────────────┐
              │  同一套前端  │   ← 只有这一份代码要维护
              └──────┬───────┘
     ┌───────┬───────┼───────┬────────┐
   网页    安卓    Win/Mac/Linux    iOS
  (原生)  (WebView壳)  (Tauri壳)   (WKWebView壳)
```

**为什么**：界面改一次，五端同时生效。反过来，写五套 = 同一个 bug 修五遍。

### 各端技术选型

| 端 | 方案 | 成本 | 卡点 |
|---|---|---|---|
| 网页 | 已有 | — | 无 |
| 安卓 APP | WebView 壳 | **低** —— 沙盒已有 APK 构建链，出过包 | 无 |
| Win / Mac / Linux | **Tauri**（Rust） | 低 —— 同一份工程出三端 | 无 |
| iOS | WKWebView 壳 | 中 | **⚠️ 见下** |

**桌面端为什么选 Tauri 不选 Electron**：Electron 打出来的包 **100MB 起**（塞了一整个浏览器），Tauri 用系统自带的 WebView，**10MB 级**。同一个界面，体积差十倍，何必。

### 「一串代码就能装」

这是你的要求，做法就一行：

```bash
curl -fsSL https://我们的地址/install.sh | sh
```

脚本自动判断系统、下对应包、装好、起服务。

**能不能做到，取决于一件现在就该定的事 ↓**

### ⚠️ 关键决策：桌面端是「瘦」还是「胖」

这个决定架构，现在不定，后面返工：

| | 瘦客户端 | **胖客户端（建议）** |
|---|---|---|
| 桌面端跑什么 | 只有界面 | **界面 + 服务端** |
| 数据在哪 | 别人的服务器 | **你自己电脑上** |
| 装完能用吗 | 要填服务器地址 | **装完就能用** |
| 对应形态 | 像网页套壳 | 像 Docker Desktop / Ollama |

**建议胖的**，两个理由：
1. **符合我们「自家的门，自家的钥匙」那条信条** —— 装完数据在自己机器上，不用先找服务器
2. **`curl | sh` 才有意义** —— 装完直接能开会话，而不是装了个空壳要你去配地址

**这反过来说明了服务端该用什么写：编译成单文件二进制。**
一个可执行文件 = Linux 能装、Windows 能装、Mac 能装。这也是为什么一期技术选型要往 **Go / Rust** 上靠，而不是 Node（Node 要拖一堆运行时）。

### iOS：我必须先跟你说清楚

**其他四端我都有把握，iOS 有硬门槛**，不是难度问题，是现实条件：

| 门槛 | 说明 |
|---|---|
| **必须有 Mac** | iOS 只能用它自己的编译链，**我们的沙盒和服务器都做不到** |
| **Apple 开发者账号** | **$99/年**（约 ¥688），不交钱只能本地自签、7 天失效 |
| 上架审核 | 或者只走 TestFlight（内测，不用审核） |

**可行解**：借用 **GitHub Actions 的 macOS 构建机**（公开仓库免费用），代码推上去自动出 iOS 包。**但账号那一步只能你本人办**，我没法替。

→ **建议把 iOS 排在最后**，等前四端跑顺了再说。它不是难，是"要花钱买门票"。

### 构建：全部交给 GitHub Actions

不在我们任何一台机器上编译 Win/Mac 包 —— 推代码，**GitHub 自动出安装包**，挂到 Release 页。

**这正好和你说的「每天在 GitHub 上更新」接上**：推一次，全平台包自动出。

### 排期建议

**别一次做五端。按「谁在用」排：**

```
一期 消息服务（地基）  ← 必须先做，多端才有东西可包
  ↓
网页（已有）＋ 安卓 APP        ← 你在用，我能出包
  ↓
Linux / Windows 桌面          ← 你电脑上
  ↓
macOS                         ← 同一个 Tauri 工程，顺带
  ↓
iOS                           ← 最后，卡在账号
```

**一句话：地基没打好之前，壳子套得越多、返工越贵。**
所以多端计划写进路线图，但**一期照旧先做**。

---

## 明确不做

- ❌ **不装现成的重型平台**。Octop 有 4000 多个文件，Mattermost 更重。我们的场景是"几个人、手机上、要快"，装一套不懂的系统是负担。
- ❌ **不做 SaaS**。开源项目的标准形态是自托管：别人 `docker compose up` 起一套自己的。**顺带把安全问题变成结构问题** —— 服务在他自己机器上，通道天生私有。
- ❌ **不为多 agent 自由对话做"无限轮"**。参考项目都设了硬上限（6 人 / 3 轮 / 10 条），说明失控是真的会发生的。

## 借鉴的边界

详见 [ATTRIBUTION.md](ATTRIBUTION.md)。

**没有复制任何项目的源代码。** 借鉴的是设计思路：有界轮次、消息去重、角色分离、确定性头像。相关项目均为 MIT / Apache-2.0 宽松协议，允许自由使用，我们仍然逐条致谢。
